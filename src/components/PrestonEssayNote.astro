<aside class="preston-note" aria-labelledby="preston-note-title">
  <div class="preston-note__eyebrow">// these ideas in practice</div>
  <h2 id="preston-note-title">These ideas in practice</h2>
  <p>
    I’m building <a href="https://preston.bot/" target="_blank" rel="noopener noreferrer">Preston ↗</a>
    to test these ideas against real software work. It maps what a codebase and its team appear to
    believe, keeps the reasons attached, and revisits what follows when those reasons change.
  </p>
</aside>

<style>
  .preston-note {
    margin-top: 56px;
    padding-top: 24px;
    border-top: 1px solid var(--c-border);
  }

  .preston-note__eyebrow {
    margin-bottom: 10px;
    color: var(--c-accent);
    font-family: 'JetBrains Mono', ui-monospace, monospace;
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .preston-note h2 {
    margin: 0;
    color: var(--c-text);
    font-family: 'JetBrains Mono', ui-monospace, monospace;
    font-size: 18px;
    font-weight: 500;
    letter-spacing: 0;
    line-height: 1.35;
  }

  .preston-note p {
    max-width: 640px;
    margin: 10px 0 0;
    color: var(--c-text-secondary);
    font-family: 'Inter', sans-serif;
    font-size: 14px;
    line-height: 1.75;
  }

  .preston-note a {
    color: var(--c-accent);
    text-decoration: underline;
    text-decoration-color: var(--c-border-strong);
    text-underline-offset: 3px;
  }

  .preston-note a:hover {
    color: var(--c-accent-hover);
    text-decoration-color: currentColor;
  }
</style>
